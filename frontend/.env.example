# Copy this file to .env.local for development or .env.production for deployment.
# Never commit real credentials or browser-exposed production secrets.

VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
# Optional convenience credentials for the local demo login flow only.
# Do not place server-only provider keys or sensitive production secrets in VITE_ variables.
VITE_BACKEND_DEMO_EMAIL=
VITE_BACKEND_DEMO_PASSWORD=
